/** Extra breathing room below scroll content, beyond the system safe area. */
export const SCREEN_BOTTOM_BUFFER = 20;

/** Default scroll content padding when the screen shell already applies bottom safe area. */
export const SCREEN_SCROLL_BOTTOM_PADDING = 24;
